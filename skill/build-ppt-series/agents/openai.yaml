interface:
  display_name: "Build PPT Series"
  short_description: "Build consistent PPT series with planned visual assets"
  default_prompt: "Use $build-ppt-series to create or improve a reusable presentation series from my source files."
policy:
  allow_implicit_invocation: true
